baseURL = "https://www.romainpoulin.fr/"
# include content marked as draft
buildDrafts = false
# include content with publishdate in the future
buildFuture = false
# include content already expired
buildExpired = false
# enable this to make all relative URLs relative to content root. Note that this does not affect absolute URLs.
relativeURLs = true
disableRSS = true
enableRobotsTXT = true
editor = "nano"
languageCode = "en"
language = "en"
theme = "vncnt-hugo"
title = "Romain POULIN"

_merge = "none"

[permalinks]
  fixed = ":title/"
  posts = "posts/:slug/"

[params]
  author = "Romain POULIN"
  # email does still work but is deprecated.
  # use [params.contact.email] instead.
  #email = "rp-pharma@pm.me"
  description = "Landing Page"
  bio = " ###### **Bienvenue sur mon site web !**  \n ###### Vous trouverez ici mon CV, mes conditions et mes disponibilités !  \n ###### Je remplace toute l’année sur toute la France donc n’hésitez pas à me contacter !   \n ###### À bientôt dans votre officine !"
  avatar = "img/avatar.jpg"
  favicon = "img/favicon.ico"
  error404 = "Oups....Erreur 404 !"

  [params.contact]
    
    # You may use (inline) tables for more versatility
    # This allows sorting and even more icons.
    # Just make sure not to mix key-value pairs, inline tables and tables,
    # although keeping keeping this order is ok.
    # See https://toml.io/en/v1.0.0 for details
    
    
    [params.contact.2] # key names are sorted lexicographically
      name = "CV" # may be omitted. defaults to key name
      icon = "user-tie" # may be omitted. defaults to key name
      style = "fas" # may be omitted. default = "fas"
      link = "https://local-hiking.club"
    [params.contact.3] # key names are sorted lexicographically
      name = "conditions" # may be omitted. defaults to key name
      icon = "search" # may be omitted. defaults to key name
      style = "fas" # may be omitted. default = "fas"
      link = "https://local-hiking.club"
    [params.contact.4] # key names are sorted lexicographically
      name = "planning" # may be omitted. defaults to key name
      icon = "calendar" # may be omitted. defaults to key name
      style = "fas" # may be omitted. default = "fas"
      link = "https://local-hiking.club"
    [params.contact.email]
      name = "email" # may be omitted. defaults to key name
      icon = "envelope" # may be omitted. defaults to key name
      style = "fas" # may be omitted. default = "fas"
      link = "mailto:rp-pharma@pm.me"
    
      
      
